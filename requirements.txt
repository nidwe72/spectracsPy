# Spectracs — desktop dependency set (all desktop platforms), pinned to the working venv.
# See requirements-android-main.txt / requirements-android-server.txt for the on-device subsets.
#
# NOT listed (removed in P1 as dead/scratch-only — docs/SPEC_android_port.md D9):
#   astropy, pandas, BaselineRemoval, rascal, scikit-image, scikit-learn
PySide6==6.5.0
pyqtgraph==0.14.0
numpy==1.26.4
scipy==1.10.1
opencv-python-headless==4.7.0.72
Pyro5==5.14
SQLAlchemy==2.0.12
SQLAlchemy-serializer==1.4.1
marshmallow==3.20.1
marshmallow-sqlalchemy==0.29.0
bcrypt==5.0.0
colour-science==0.4.2
colormath==3.0.0
luxpy==1.9.8
spectres==2.2.0
rgbxy==0.5
pyspectra==0.0.1.2
psutil==5.9.5
pyusb==1.2.1
appdata==2.2.0
# PDF report (M2 — SPEC_bench_pdf_export.md): matplotlib renders the pages, pypdf embeds the
# whole-Workflow JSON + captured images as named attachments. matplotlib/Pillow were present in the
# working venv transitively; pinned here now that they are a declared dependency.
matplotlib==3.7.1
Pillow==9.5.0
pypdf==6.14.2
# Schema migrations (SPEC_schema_migrations.md): Alembic runs at boot in BOTH the app and server processes
# (DatabaseInitializer). Mako/MarkupSafe are its transitive deps.
alembic==1.18.5
# Plugin signing (SPEC_plugin_distribution.md B2): PyNaCl signs on the master (desktop only); clients VERIFY
# with a pure-Python Ed25519 vendored in -core, so Android never links libsodium. cffi/pycparser are its deps.
PyNaCl==1.6.2
